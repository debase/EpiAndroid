<resources>

    <!-- Base application theme. -->
    <style name="NoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>

        <!-- colorPrimaryDark is used for the status bar (with the
             battery, clock, etc). -->
        <item name="colorPrimaryDark">@color/primaryColorDark</item>

    </style>

    <style name="AppThemeLogin" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/LoginActionBar</item>
        <item name="actionBarStyle">@style/LoginActionBar</item>
    </style>

    <!-- Base application theme.
         Defining this lets values-v21/styles.xml reuse it with changes. -->

    <style name="LoginActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/primaryColor</item>
        <item name="background">@color/primaryColor</item>
    </style>
</resources>
